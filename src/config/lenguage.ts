export const DEFAULT_LANGUAGE = 'en'

export function getLanguage(): string {
  // You can implement logic here to determine the language
  // based on user preferences, browser settings, etc.
  return DEFAULT_LANGUAGE
}

